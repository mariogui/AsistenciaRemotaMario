/* ============================================
   Configuración de Tracking (pública)
   Los secretos están en Cloudflare Workers
   ============================================ */

const TrackingConfig = {
  RATE_LIMIT_MS: 5000,
  ENABLED: true,
};
